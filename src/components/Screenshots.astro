---
---
<section class="py-24 relative" id="screenshots">
  <div class="absolute inset-0 bg-gradient-to-b from-transparent via-emerald-950/10 to-transparent"></div>

  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16 fade-in">
      <h2 class="text-3xl sm:text-4xl font-bold">
        See it in
        <span class="bg-gradient-to-r from-emerald-400 to-emerald-600 bg-clip-text text-transparent">action</span>
      </h2>
      <p class="mt-4 text-lg text-gray-400 max-w-2xl mx-auto">
        Monitor and control your AI coding sessions from your desktop or phone.
      </p>
    </div>

    <div class="flex flex-col lg:flex-row gap-12 items-end justify-center">
      <!-- Desktop screenshot (primary, larger) -->
      <div class="fade-in lg:flex-1 max-w-2xl">
        <div class="rounded-xl overflow-hidden border border-gray-700/50 shadow-2xl shadow-emerald-900/10">
          <div class="bg-gray-800 px-4 py-2.5 flex items-center gap-2">
            <div class="flex gap-1.5">
              <div class="w-3 h-3 rounded-full bg-red-500/80"></div>
              <div class="w-3 h-3 rounded-full bg-yellow-500/80"></div>
              <div class="w-3 h-3 rounded-full bg-green-500/80"></div>
            </div>
            <span class="text-xs text-gray-400 ml-2 font-mono">tmux-remote attach cli 1</span>
          </div>
          <img src="/screenshots/desktop-terminal.png" alt="Desktop terminal showing Claude Code running via tmux-remote" class="w-full" loading="lazy" />
        </div>
        <p class="mt-4 text-center text-gray-400 text-sm">
          <strong class="text-gray-300">Desktop CLI</strong> &middot; Attach to remote tmux sessions from your terminal. Full-fidelity rendering, exactly like being local.
        </p>
      </div>

      <!-- iOS screenshot (secondary, constrained height) -->
      <div class="fade-in flex flex-col items-center flex-shrink-0">
        <div class="w-48 sm:w-56 max-h-[28rem] rounded-3xl overflow-hidden border-2 border-gray-700/50 shadow-2xl shadow-emerald-900/10 bg-black">
          <img src="/screenshots/ios-terminal.png" alt="iOS app showing Claude Code running in tmux-remote" class="w-full h-full object-cover object-top" loading="lazy" />
        </div>
        <p class="mt-4 text-center text-gray-400 text-sm max-w-xs">
          <strong class="text-gray-300">iOS App</strong> &middot; Full terminal access from your iPhone. Interact with Claude Code, approve commands, and monitor progress on the go.
        </p>
      </div>
    </div>
  </div>
</section>
